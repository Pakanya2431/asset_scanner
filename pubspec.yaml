name: asset_scanner
description: Fixed Asset Verification System
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  mobile_scanner: ^5.2.3
  excel: ^4.0.6
  path_provider: ^2.1.4
  share_plus: ^10.1.4
  intl: ^0.19.0
  permission_handler: ^11.3.1
  file_picker: ^8.1.2
  provider: ^6.1.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
